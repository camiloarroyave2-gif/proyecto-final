databases:
  - name: taskflow-db
    plan: free
    region: ohio
    postgresMajorVersion: 16

services:
  - type: web
    name: taskflow
    runtime: docker
      - key: DATABASE_URL
        fromService:
          type: postgresql
          name: taskflow-db
          property: connectionString
